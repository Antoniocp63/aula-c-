// Online C compiler to run C program online
#include <stdio.h>

int main() {
    float numeros[6];
    int pos,
    float soma = 0, media;
    
    for (pos = 0; pos < 6; pos++){
     printf("Digite o número da posição %d: ", pos);
     scanf("%d", &numeros[pos]);
    }
    
    for (pos = 0; pos < 6; pos++){
        media = ()
    
    printf("Média = %.2f\n", media);
    return 0;
}